<!DOCTYPE html>
<html>
<head>
  <title><%= @video.file.filename %></title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <style>
    .card {
      width: 300px; /* Ancho de la tarjeta */
      margin: auto; /* Centrar la tarjeta */
      margin-top: 20px; /* Espacio superior */
    }
    video {
      width: 100%; /* Ancho del video al 100% de la tarjeta */
    }
  </style>
</head>
<body>
  <div class="container mt-4">
    <h1 class="fancy-title"><%= @video.title %></h1>
    <div class="card">
      <div class="card-body">
        <video controls>
          <source src="<%= rails_blob_path(@video.file) %>" type="video/mp4">
          Your browser does not support the video tag.
        </video>
      </div>
    </div>
    <a href="<%= videos_path %>" class="btn btn-primary mt-3">Volver atrás</a>
  </div>
</body>
</html>
