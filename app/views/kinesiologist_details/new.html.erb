<h1>Crear Anuncio</h1>

<%= form_with model:@kinesiologist_detail , local: true do |f| %>
  <div class="form-group mb-3">
    <%= f.label :specialty, "Especialidad" %>
    <%= f.text_field :specialty, class: "form-control", required: true %>
  </div>
  <div class="form-group mb-3">
    <%= f.label :description, "Descripción" %>
    <%= f.text_field :description, class: "form-control", required: true %>
  </div>
  <div class="form-group mb-3">
    <%= f.label :user_id %>
    <%= f.collection_select :user_id, User.all, :id, :username, {prompt: "Selecciona un usuario"}, {class: "form-control"} %>
  </div>
  <%= f.submit "Guardar" %>
<% end %>
