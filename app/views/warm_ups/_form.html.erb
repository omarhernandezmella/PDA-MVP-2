<%= form_with model: @warm_up, local: true do |form| %>
  <div class="form-group mb-3">
    <%= form.label :exercise, 'Ejercicio' %>
    <%= form.text_field :exercise, class: "form-control", required: true %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :training_id, 'Entrenamiento' %>
    <%= form.collection_select :training_id, Training.all, :id, :exercise, { prompt: 'Seleccione el ejercicio' }, class: "form-control", required: true %>
  </div>

  <div class="form-group mb-3">
    <%= form.label :youtube_video_id, "Id video YouTube" %>
    <%= form.text_field :youtube_video_id, class: 'form-control' %>
  </div>

  <%= form.submit "Guardar", class: "btn btn-primary" %>
<% end %>
